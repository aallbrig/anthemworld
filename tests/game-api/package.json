{
  "name": "anthem-game-api-tests",
  "version": "1.0.0",
  "description": "Integration tests for Anthem World game API",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:verbose": "node --test --reporter=spec tests/*.test.js"
  },
  "dependencies": {},
  "engines": { "node": ">=20" }
}
