{
  "name": "anthem-game-api-tests",
  "version": "1.0.0",
  "description": "Integration tests for Anthem World game API",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:verbose": "node --test --reporter=spec tests/*.test.js",
    "test:security": "node --test --reporter=spec tests/security.test.js",
    "test:config": "node --test --reporter=spec tests/config.test.js"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "js-yaml": "^4.1.0"
  },
  "engines": { "node": ">=20" }
}
