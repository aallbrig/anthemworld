{
  "name": "anthem-game-functions",
  "version": "1.0.0",
  "description": "Lambda functions for Anthem World game backend",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.0",
    "jest": "^29.0.0"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint . --ignore-pattern 'node_modules/**'"
  },
  "jest": {
    "testMatch": ["**/__tests__/**/*.test.js"]
  }
}
